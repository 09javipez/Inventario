{
    "buildCommand": "curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-interaction --prefer-dist --optimize-autoloader && npm run build",
    "outputDirectory": "public"
}
